<?xml version="1.0" encoding="utf-8"?>
<!--
    Layout: Splash Screen
    Deskripsi: Menampilkan animasi Lottie dan konten tambahan (bendera & sapaan)
-->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/rootLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/md_theme_light_primary"
    tools:context=".ui.splash.SplashActivity"
    tools:ignore="ExtraText">

    <!-- ================= LOTTIE SPLASH ANIMATION ================= -->
    <com.airbnb.lottie.LottieAnimationView
        android:id="@+id/lottieAnimationView"
        android:layout_width="300dp"
        android:layout_height="300dp"
        android:layout_marginBottom="32dp"
        app:lottie_autoPlay="true"
        app:lottie_loop="true"
        app:lottie_rawRes="@raw/splash_animation"
        app:layout_constraintBottom_toTopOf="@+id/flagContainer"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_chainStyle="packed" />

    <!-- ================= FLAG & GREETING CONTAINER ================= -->
    <!--
        Container untuk gambar bendera dan teks sapaan.
        Menggunakan FrameLayout agar elemen tidak saling menggeser.
    -->
    <FrameLayout
        android:id="@+id/flagContainer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:visibility="gone"
        app:layout_constraintTop_toBottomOf="@id/lottieAnimationView"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent">

        <!-- Flag Image -->
        <ImageView
            android:id="@+id/imgFlag"
            android:layout_width="150dp"
            android:layout_height="100dp"
            android:adjustViewBounds="true"
            android:contentDescription="@string/flag_desc"
            android:layout_gravity="center"
            android:scaleType="centerCrop"
            android:visibility="gone" />

        <!-- Greeting Text -->
        <TextView
            android:id="@+id/tvGreeting"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|center_horizontal"
            android:layout_marginTop="110dp"
            android:gravity="center"
            android:paddingTop="24sp"
            android:text="@string/hello_message"
            android:textColor="@color/md_theme_light_onBackground"
            android:textSize="24sp"
            android:textStyle="bold"
            android:visibility="gone" />

    </FrameLayout>

</androidx.constraintlayout.widget.ConstraintLayout>