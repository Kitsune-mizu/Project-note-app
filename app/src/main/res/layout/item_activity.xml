<?xml version="1.0" encoding="utf-8"?>
<!--
    Layout Item: Activity List (Modern)
    Icon dibungkus container rounded untuk kesan lebih modern
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@android:color/transparent"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingHorizontal="4dp"
    android:paddingVertical="10dp">

    <!-- Icon Container rounded -->
    <FrameLayout
        android:layout_width="38dp"
        android:layout_height="38dp"
        android:background="@drawable/bg_icon_dialog">

        <ImageView
            android:id="@+id/iconActivity"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_gravity="center"
            android:contentDescription="@string/cd_activity_icon"
            app:srcCompat="@drawable/ic_check_circle"
            app:tint="?attr/text_color" />
    </FrameLayout>

    <!-- Title & Desc -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tvActivityTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/activity_title_placeholder"
            android:fontFamily="@font/linottesemibold"
            android:textAppearance="?attr/textAppearanceBodyMedium"
            android:textColor="?attr/text_color"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tvActivityDesc"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:text="@string/activity_description_placeholder"
            android:fontFamily="@font/linottesemibold"
            android:textAppearance="?attr/textAppearanceLabelSmall"
            android:textColor="?attr/text_color"
            android:alpha="0.6"/>

    </LinearLayout>

    <!-- Time -->
    <TextView
        android:id="@+id/tvActivityTime"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/activity_time_placeholder"
        android:fontFamily="@font/linottesemibold"
        android:textAppearance="?attr/textAppearanceLabelSmall"
        android:textColor="?attr/text_color" />

</LinearLayout>