<?xml version="1.0" encoding="utf-8"?>
<!--
    Layout: MainActivity DrawerLayout (Modern)
    ──────────────────────────────────────────────────────────────────
    itemIconTint / itemTextColor → @color/nav_item_color
      File: res/color/nav_item_color.xml
      state_checked=true  → ?attr/colorPrimary
      default             → ?attr/colorOnSurfaceVariant

    itemShapeFillColor → @color/nav_item_shape_color
      File: res/color/nav_item_shape_color.xml
      state_checked=true  → ?attr/colorPrimaryContainer
      default             → @android:color/transparent
    ──────────────────────────────────────────────────────────────────
-->
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.main.MainActivity"
    tools:ignore="ExtraText">

    <!-- ── MAIN CONTENT ── -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="?attr/colorSurface"
            android:elevation="0dp"
            app:navigationIconTint="?attr/colorOnSurface"
            app:popupTheme="@style/ThemeOverlay.Material3.Light"
            app:titleTextColor="?attr/colorOnSurface" />

        <!-- Divider tipis di bawah toolbar -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:alpha="0.08"
            android:background="?attr/colorOnSurface" />

        <FrameLayout
            android:id="@+id/fragment_container"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="?attr/colorSurface" />

    </LinearLayout>

    <!-- ── NAVIGATION DRAWER ── -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="300dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="?attr/colorSurface"
        app:headerLayout="@layout/nav_header"
        app:itemIconTint="@color/nav_item_color"
        app:itemRippleColor="?attr/colorPrimaryContainer"
        app:itemShapeFillColor="@color/nav_item_shape_color"
        app:itemTextColor="@color/nav_item_color"
        app:menu="@menu/nav_menu_main" />

</androidx.drawerlayout.widget.DrawerLayout>